import { IPlayerModel } from '@deporty-org/entities/players';
import { TransversalContract } from '../core/transversal-contract';

export abstract class PlayerContract extends TransversalContract<IPlayerModel> {
}
