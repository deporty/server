import { PermissionEntity } from '@deporty-org/entities/authorization';
import { TransversalContract } from '../../../core/transversal-contract';

export abstract class PermissionContract extends TransversalContract<PermissionEntity> {}
